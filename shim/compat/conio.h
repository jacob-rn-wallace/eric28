/*
 *   conio.h
 *
 *   Part of the Emu28 macOS/Linux port's compat/ shim directory (see
 *   ../../CLAUDE.md). Stands in for the MSVC CRT header so that
 *   vendor/emu28-upstream/PCH.H's `#include <conio.h>` resolves on
 *   macOS/Linux. Nothing in the current build scope (the CPU/RPL
 *   engine) calls its console I/O functions - empty until something
 *   needs it.
 */

#ifndef SHIM_COMPAT_CONIO_H
#define SHIM_COMPAT_CONIO_H

#endif /* SHIM_COMPAT_CONIO_H */
